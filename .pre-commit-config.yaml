# Pre-commit hooks for reflectt-node
# Install: pip install pre-commit && pre-commit install
# Or: brew install pre-commit && pre-commit install

repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.21.2
    hooks:
      - id: gitleaks
