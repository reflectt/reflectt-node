# QA Bundle: task-1771872643209-f8c6nq3s8

## Summary
Onboarding telemetry dashboard: step-by-step conversion funnel, failure-reason
distribution per step, and weekly trend snapshot export.

## Endpoints
- `GET /activation/dashboard` — combined dashboard snapshot
- `GET /activation/funnel/conversions` — per-step rates + median timing
- `GET /activation/funnel/failures` — drop-off reasons from event metadata
- `GET /activation/funnel/weekly` — ISO-week trend snapshots (exportable JSON)

## Changes
- `src/activationEvents.ts`: 4 dashboard functions + types + preflight events
- `src/server.ts`: 4 endpoints + event type validation update
- `tests/activationEvents.test.ts`: 10 new dashboard tests
- `public/docs.md`: endpoint docs updated

## Test Results
- 894 tests pass (53 files)
