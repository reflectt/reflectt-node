# list_portals

Returns the portals available within a space. The tool searches the `data/spaces/<space>/portals` directory for portal definitions and falls back to the Concierge portal when no custom portals are present.
