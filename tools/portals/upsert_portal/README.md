# upsert_portal

Creates or updates a portal definition inside a space. The tool writes `portal.json`, refreshes timestamps, and (optionally) ensures the standard folder structure—`pages/`, `workflows/`, `integrations/`—exists for the portal so other tools can attach resources immediately.
