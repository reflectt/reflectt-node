# delete_portal

Deletes an existing portal by removing its directory (and any nested resources) from the target space. Use the optional `force` flag if you truly need to drop a reserved portal like `concierge`.
