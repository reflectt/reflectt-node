{
  "id": "search_components",
  "name": "Search Components",
  "description": "Search all 176 UI components by natural language query using semantic similarity search (AI-powered embeddings) with automatic fallback to keyword search. Returns brief summaries of matching components with relevance scores. Use load_components to get full schemas.",
  "category": "discovery",
  "function_name": "search_components",
  "parameters": {
    "type": "object",
    "required": ["query"],
    "properties": {
      "query": {
        "type": "string",
        "description": "Natural language search query (e.g., 'data table', 'calendar', 'user profile')"
      },
      "domain": {
        "type": "string",
        "description": "Optional domain filter (e.g., 'office', 'data', 'collaboration')"
      },
      "limit": {
        "type": "number",
        "default": 10,
        "minimum": 1,
        "maximum": 50,
        "description": "Maximum number of results"
      }
    }
  },
  "use_tool_context": true,
  "examples": [
    {"query": "data table", "limit": 5},
    {"query": "calendar", "domain": "office"}
  ],
  "tags": ["discovery", "search", "components", "ui"],
  "version": "1.0.0"
}
